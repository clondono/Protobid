<%= form_for(@bid) do |f| %>
  <% if @bid.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bid.errors.count, "error") %> prohibited this bid from being saved:</h2>

      <ul>
      <% @bid.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
  <div style="float:left; margin-right:5px"><%= f.label "Cost" %>
  <%= f.text_field :cost%></div>
  
  <div style="float:left; margin-right:5px"><%= f.label "Time" %>
  <%= f.text_field :time%></div>
  <%= f.hidden_field :vendorid, :value => @vendorID%></div>
  <%= f.hidden_field :prototypeid, :value => @prototypeID%></div>
  <div class="actions">
    <%= f.submit() %>
  </div>
<% end %>
