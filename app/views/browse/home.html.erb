<% provide(:title, "Home") %>
</br>

<div class="pages">
	<% if user_signed_in? %>
		<h2>
			Welcome, <%= current_user.firstname %> <%= current_user.lastname%>
		</h2>
	<% else%>
		<p>
			Welcome! Please sign in to explore Protobid :)
		</p>
	<%end %>
</div>